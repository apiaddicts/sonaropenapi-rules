openapi: "3.0.0"
info:
  version: 1.0.0
  title: Swagger Petstore
paths:
  /examples:
    get: # in scope but not paginated (no 206 response) -> rule does not apply
      responses:
        200:
          description: Ok

openapi: "3.0.0"
info:
  version: 1.0.0
  title: OAR060 non-query params
paths:
  /pets/{id}:
    get:
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
        - in: header
          name: X-Trace
          required: true
          schema:
            type: string
        - in: cookie
          name: session
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK

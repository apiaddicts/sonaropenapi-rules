openapi: "3.1.0"
info:
  version: 1.0.0
  title: Sample API
paths:
  /pets:
    get:
      responses:
        "200":
          description: Ok
  /pets/{id}:
    get:
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Ok

    swagger: "2.0"
    info:
      version: "1.0.0"
      title: "API que no cumple"
    paths:
      /usuarios:
        get:
          parameters:
            - name: username
              in: query
              type: string
            - name: user_first_name
              in: query
              type: string
          responses:
            200:
              description: Ok
swagger: "2.0"
info:
  version: 1.0.0
  title: Swagger Petstore
paths:
  /examples:
    get:
      parameters:
        - in: query
          name: $limit
          type: integer
        - in: query
          name: $limit
          type: integer
      responses:
        206:
          description: Ok

swagger: "2.0"
info:
  version: 1.0.0
  title: Swagger Petstore
parameters:
  limit:
    in: query
    name: $limit
    type: integer
    format: int64
paths:
  /examples:
    get:
      parameters:
        - $ref: '#/parameters/limit'
      responses:
        206:
          description: Ok

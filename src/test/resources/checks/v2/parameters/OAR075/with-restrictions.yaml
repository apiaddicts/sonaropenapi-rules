swagger: '2.0'
info:
  title: API con restricciones
  version: 1.0.0
paths:
  /users/{id}:
    get:
      parameters:
        - name: id
          in: path
          required: true
          type: string
          maxLength: 10
          enum: ['admin', 'user', 'guest']
      responses:
        '200':
          description: Un usuario
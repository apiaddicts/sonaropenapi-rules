swagger: "2.0"
info:
  version: 1.0.0
  title: Test
paths:
  /test:
    get:
      responses:
        "200":
          description: ok
          schema:
            type: object
            properties:
              data: # Noncompliant {{OAR029: At least one property must be defined}} {{OAR029: 'id' property is missing}}
                type: object

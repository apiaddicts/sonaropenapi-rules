swagger: "2.0"
info:
  version: 1.0.0
  title: Test
paths:
  /test:
    get:
      responses:
        "200":
          description: ok
          schema:
            type: object
            properties:
              payload: # Noncompliant {{OAR029: At least one property must be defined}}
                type: object
